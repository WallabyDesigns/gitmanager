<div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
    <div>
        <h2 class="text-xl font-semibold text-slate-100">{{ __('Processes') }}</h2>
        <p class="text-sm text-slate-400">{{ __('Active deployments, node services, and task queue.') }}</p>
    </div>
</div>
