<div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
    <div>
<h2 class="text-xl font-semibold text-slate-100">{{ __('New Project') }}</h2>
        <p class="text-sm text-slate-400">{{ __('Add a new deployment target and connect it to Git.') }}</p>
        <p>{{ __('Back to projects') }}</p>
    </a>
</div>

